apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cornextgen.fullname" . }}-test-connection"
  labels:
    {{- include "cornextgen.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cornextgen.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
