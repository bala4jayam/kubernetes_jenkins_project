apiVersion: v1
kind: Service
metadata:
  name: {{ include "cornextgen.fullname" . }}
  labels:
    {{- include "cornextgen.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cornextgen.selectorLabels" . | nindent 4 }}
