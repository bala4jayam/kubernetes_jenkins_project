apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "camel-discover-sftp-integration.fullname" . }}-test-connection"
  labels:
    {{- include "camel-discover-sftp-integration.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "camel-discover-sftp-integration.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
