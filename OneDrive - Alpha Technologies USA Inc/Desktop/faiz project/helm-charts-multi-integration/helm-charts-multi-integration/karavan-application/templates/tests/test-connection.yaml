apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "karavan-application.fullname" . }}-test-connection"
  labels:
    {{- include "karavan-application.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "karavan-application.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
